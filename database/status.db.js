const statusOpen =()=> "aberta"
const statusPaused =()=> "pausada"
const statusCancel =()=> "cancelada"
const statusFinalized =()=> "finalizada"

module.exports = {
  statusOpen, 
  statusPaused,
  statusCancel,
  statusFinalized
}
